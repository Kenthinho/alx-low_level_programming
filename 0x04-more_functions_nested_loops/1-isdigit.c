#include <ctype.h>
#include "main.h"

/**
 * main - checks the variable c if digit
 * returns 1 if true otherwise 0
 * Return: Always 0
 */

int _isdigit(int c)
{
if (isdigit(int c))
{
return (1);
}
else
{
return (0);
}
}
